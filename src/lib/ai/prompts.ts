export const buildPrompt = (userPrompt: string, style: string) =>
  `${userPrompt}, ${style} style, isolated design on transparent background, ` +
  `high contrast, print-ready, vector illustration aesthetic, no text, no watermark`;
